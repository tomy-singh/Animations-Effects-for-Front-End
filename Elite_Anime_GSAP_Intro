
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Elite Anime Scroll — 10 Scenes</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="stylesheet" href="style.css">
</head>
<body>

<!-- CINEMATIC BACKGROUND -->
<div class="cinematic-bg"></div>
<div class="grain"></div>

<!-- INTRO -->
<section class="intro">
  <h1 class="title">
    <span>N</span><span>E</span><span>O</span>
    <span>&nbsp;</span>
    <span>A</span><span>N</span><span>I</span><span>M</span><span>E</span>
  </h1>
  <p class="subtitle">覚醒する未来</p>
</section>

<!-- SCENES -->
<div class="scenes">

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene1.jpg" alt="scene 1">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene2.jpg" alt="scene 2">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene3.jpg" alt="scene 3">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene4.jpg" alt="scene 4">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene5.jpg" alt="scene 5">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene6.jpg" alt="scene 6">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene7.jpg" alt="scene 7">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene8.jpg" alt="scene 8">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene9.jpg" alt="scene 9">
    </div>
  </section>

  <section class="scene">
    <div class="image-wrap">
      <img src="images/scene10.jpg" alt="scene 10">
    </div>
  </section>

</div>

<script src="https://unpkg.com/gsap@3/dist/gsap.min.js"></script>
<script src="https://unpkg.com/gsap@3/dist/ScrollTrigger.min.js"></script>
<script src="script.js"></script>
</body>
</html>

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


* { box-sizing: border-box; }

body {
  margin: 0;
  background: #020207;
  font-family: Poppins, sans-serif;
  overflow-x: hidden;
  color: white;
}

/* BACKGROUND */
.cinematic-bg {
  position: fixed;
  inset: 0;
  background: radial-gradient(circle at 30% 10%, #1b2b6f, #020207 70%);
  z-index: -2;
}

.grain {
  position: fixed;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4'/%3E%3C/filter%3E%3Crect width='200' height='200' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
  pointer-events: none;
  z-index: 10;
}

/* INTRO */
.intro {
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.title span {
  display: inline-block;
  font-size: 4rem;
  text-shadow: 0 0 30px rgba(120,160,255,.8);
}

.subtitle {
  letter-spacing: .4em;
  opacity: .8;
}

/* SCENES */
.scene {
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  perspective: 1400px;
}

.image-wrap {
  width: 65vw;
  height: 60vh;
  position: relative;
  background: linear-gradient(120deg, rgba(120,160,255,0.15), rgba(20,30,60,0.85));
  box-shadow: 0 0 60px rgba(120,160,255,0.25);
  transform-style: preserve-3d;
  overflow: hidden;
}

.image-wrap img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* Placeholder sweep */
.image-wrap::before {
  content: "";
  position: absolute;
  inset: -40%;
  background: linear-gradient(120deg, transparent, rgba(255,255,255,.15), transparent);
  animation: sweep 4s infinite linear;
}

@keyframes sweep {
  from { transform: translateX(-100%); }
  to { transform: translateX(100%); }
}

//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


gsap.registerPlugin(ScrollTrigger);

/* INTRO */
gsap.timeline()
  .from(".title span", {
    opacity: 0,
    y: 120,
    rotateX: 70,
    stagger: 0.08,
    duration: 1.4,
    ease: "power4.out"
  })
  .from(".subtitle", {
    opacity: 0,
    y: 40,
    duration: .8
  }, "-=.6")
  .to(".intro", {
    yPercent: -100,
    duration: 1.2,
    ease: "expo.inOut",
    delay: .6
  });

/* SCENES */
gsap.utils.toArray(".image-wrap").forEach((wrap, i) => {

  gsap.fromTo(wrap,
    {
      opacity: 0,
      y: 120,
      rotateY: i % 2 ? -15 : 15,
      clipPath: "circle(30% at 50% 50%)"
    },
    {
      opacity: 1,
      y: 0,
      rotateY: 0,
      clipPath: "polygon(8% 0%,92% 0%,100% 8%,100% 92%,92% 100%,8% 100%,0% 92%,0% 8%)",
      ease: "power4.out",
      scrollTrigger: {
        trigger: wrap,
        start: "top 75%",
        end: "top 30%",
        scrub: true
      }
    }
  );
});

